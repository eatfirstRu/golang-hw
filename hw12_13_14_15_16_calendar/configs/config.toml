[logger]
level = "INFO"

[storage]
type = "memory"

[database]
host = "localhost"
port = 5432
user = "calendar"
password = "calendar"
name = "calendar"
ssl_mode = "disable"

[http_server]
host = "0.0.0.0"
port = 8080
